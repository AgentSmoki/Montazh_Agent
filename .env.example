# ============ TeleTranscribe (обязательно) ============
# Транскрипция: TeleTranscribe (GigaAM, диаризация, word-timestamps).
# API-ключ и MCP-сервер для подключения выдаёт бот: https://t.me/TeleTranscribe_bot
# (команда /api). Впиши сюда полученные ключ и URL.
TT_API_KEY=tt_xxxxxxxxxxxxxxxx
TT_API_BASE_URL=http://localhost:8000

# ============ Генерация B-roll / image (опционально) ============
# Higgsfield — единый gateway к Veo 3.1 / Kling 3.0 / Hailuo 02 / Nano Banana Pro / Flux 2.
# OAuth через https://higgsfield.ai/mcp, 150 free credits/мес.
# Если используется HTTP-MCP, ключ не нужен — авторизация в Claude Code.
HIGGSFIELD_API_KEY=

# Fal.ai — 1000+ моделей одной точкой (опционально, если хочется альтернативу Higgsfield).
FAL_KEY=

# ============ TTS / музыка (опционально, для generative-only mode) ============
# Нужно только если используешь TTS-озвучку (когда нет своего голосового исходника).
ELEVENLABS_API_KEY=

# Suno MCP (AceDataCloud/SunoMCP) — только если регистрируешь этот MCP-сервер.
# helpers/music_gen.py этот ключ не читает — ему нужны ключи из блока «Фоновая музыка».
SUNO_API_KEY=

# ============ Мемы (skill meme-inserter, helpers/meme_fetch.py) ============
# KLIPY — видео-мемы и GIF. Бесплатно, коммерческое использование разрешено.
KLIPY_API_KEY=

# ============ Фоновая музыка (skill video-music, helpers/music_gen.py) ============
# sunoapi.org — основной проверенный провайдер. API формально требует callBackUrl:
# подойдёт любой публичный https-URL, результат забирается поллингом record-info.
SUNOAPI_ORG_KEY=
SUNO_CALLBACK_URL=https://example.com/suno-callback
# apiframe.ai (Suno-шлюз) — опционально.
APIFRAME_KEY=
# acedata.cloud (Suno-шлюз) — опционально, нужен баланс на аккаунте; в PROVIDERS пока не реализован.
ACEDATA_SUNO_KEY=

# ============ Anthropic (не обязательно — Claude Code сам разруливает) ============
# Только если ты вызываешь Claude API из helper-скриптов напрямую (мы не вызываем).
ANTHROPIC_API_KEY=
